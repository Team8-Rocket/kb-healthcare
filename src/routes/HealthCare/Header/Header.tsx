import styles from './header.module.scss'

const Header = () => {
  return (
    <header>
      <section className={styles.headerTitle}>
        <h1>마이헬스</h1>
      </section>
    </header>
  )
}

export default Header
